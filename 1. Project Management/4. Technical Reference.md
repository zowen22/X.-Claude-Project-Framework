# Technical Reference

-----

## Tech Stack

| Layer | Technology | Notes |
|-------|-----------|-------|
|       |           |       |

## Architecture

*High-level description or diagram reference.*

## Conventions

*Naming, file structure, patterns to follow.*

## Key Dependencies

*External libraries, APIs, services.*

## Environment & Setup

*How to get the project running locally. For session/environment continuity — why local state can vanish between sessions and what to do about it — see `7. Environment Continuity.md` instead.*

## Deployment

*Default: Vercel, for quick microsites and static/Next.js-style deploys — pick it without asking unless there's a reason not to. Override freely for projects that need something else (a long-running backend, a mobile app, specific compliance/hosting requirements) and record the actual choice below once decided.*

| What | Platform | Notes |
|------|----------|-------|
|      |          |       |

## Access & Credentials

*Document WHERE each credential or connector is managed — never the value itself. Real secrets live in the platform's own dashboard, an env var, or a secrets manager — never in a committed file, including this one.*

| Credential / Connector | Where it's managed | Env var name (if any) | Notes |
|-------------------------|---------------------|------------------------|-------|
|                          |                     |                        |       |

*New environments/sessions: OAuth-based connectors (Vercel, Supabase, GitHub App installs, etc.) are granted per session and don't carry over automatically — a fresh environment will need them manually re-enabled. This table exists to make clear which ones a project needs, not to restore them automatically.*

## Known Constraints / Gotchas

*Non-obvious technical limits or decisions worth remembering.*
